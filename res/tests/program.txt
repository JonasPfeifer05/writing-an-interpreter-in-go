let fibo = fn(n) {
    if (n < 1) { ret err("Cannot compute a fibonacci number < 1!"); }
    if (n == 1 || n == 2) { ret 1; }
    fibo(n-1) + fibo(n-2)
};

let even = fn(x) {
    x % 2 == 0
};

let num = int(input("Enter a number: "));

print(num);

print(fibo(num));

print(even(num));

1